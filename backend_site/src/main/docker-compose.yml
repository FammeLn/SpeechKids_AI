version: '3.8'
services:
  db:
    image: postgres:15
    container_name: halfi-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: naodf00sazxve_dsopkaslf_dkcopxz29e
      POSTGRES_DB: halfi_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data





volumes:
  postgres_data: