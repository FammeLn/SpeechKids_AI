spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/halfi_user_db
    username: HalfiPostgres
    password: na4uih245huvz13ie9_iovjoid34_23dsacxz
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8084 # Твой порт бэкенда